all:
  children:
    managers:
      hosts:
        host-1: 
    workers:
      hosts:
        host-2:
    nginx:
      hosts:
        nginx: 
  vars:
    ansible_user: jenkins 
    ansible_ssh_private_key_file: '~/.ssh/id_rsa'
